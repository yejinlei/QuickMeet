## User request

拆解 QuickMeet 总控 Epic 下的 17 个子 Issue（QM-001 ~ QM-017）。以下是每个子 Issue 的标题、指派对象、标签、前置依赖与完整正文规格（任务背景 / 交付内容 / 强制约束 / 验收标准）。各子 Issue 需关联 Epic「QuickMeet 总控Epic」。

依赖链概览：QM-001 → QM-002 → (QM-003, QM-004) → QM-005 → (QM-006, QM-007, QM-009, QM-011) → QM-008(QM-007 后)、QM-010(QM-007 后)、QM-012(QM-011 后) → QM-013/QM-014(QM-012 后)；QM-015 前置 QM-003 并随各服务开发持续更新；QM-016 前置所有核心功能完成；QM-017 前置 QM-016。

---

### 📌 QM-001 子Issue

**标题**：chore: 初始化Rust异步项目骨架，验证webrtc-rs原生示例可运行
**指派对象**：后端开发智能体
**标签**：`chore` `environment` `webrtc-rs` `good-first-issue`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：无（项目首个启动任务）

#### 任务背景
QuickMeet项目首个基础环境搭建任务，目标是跑通webrtc-rs v0.17.1的基础编译与原生demo验证，确认底层环境可用。

#### 交付内容
1. 初始化完整Rust异步项目Cargo结构，锁定webrtc-rs v0.17.1、tokio 1.0+稳定版依赖
2. 跑通webrtc-rs原生simple_peer点对点demo，实现双浏览器tab音视频互通
3. 验证VP8/H.264视频编码、Opus音频编码兼容性，无花屏、音频中断问题
4. 输出基础Dockerfile + docker-compose.yml配置，兼容docker-compose 1.29.2版本
5. 编写最简启动说明，明确端口映射与浏览器验证方式

#### 强制约束
- 代码兼容Rust 1.75+稳定版，无私有不可访问依赖
- 服务默认监听8080端口，适配内网192.168.0.0/24网段
- 镜像构建无编译错误，docker-compose up可一键拉起demo

#### 验收标准
1. cargo build编译无错误、无警告
2. 本地启动后，两个浏览器tab打开测试页可正常互通音视频
3. VP8、H.264编码切换均正常，Opus音频清晰无中断
4. Docker镜像可正常构建运行，端口映射正确

---

### 📌 QM-002 子Issue

**标题**：feat: 实现SFU选择性转发单元核心架构，支持单流多用户分发
**指派对象**：后端开发智能体
**标签**：`feature` `sfu` `core`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-001

#### 任务背景
搭建QuickMeet核心SFU媒体转发底座，实现单上行流按需分发给所有参会者，相比MCU架构降低服务器算力消耗。

#### 交付内容
1. 实现选择性转发单元核心逻辑，支持单路上行音视频流分发给多个下行参会者
2. 音视频轨道解耦管理，摄像头流与屏幕共享流独立控制，切换流时音频传输不中断
3. 内置STUN公网NAT探测，预留TURN中继服务配置入口
4. 提供最简HTTP测试页面，支持多人同时入会验证
5. 配套更新Docker配置，单容器即可运行SFU服务

#### 强制约束
- 基于webrtc-rs原生RTCPeerConnection实现，不引入第三方SFU框架
- 支持≥3人同时入会，音视频互通正常
- 兼容浏览器原生WebRTC API，无需修改前端即可接入

#### 验收标准
1. 3个浏览器tab同时入会，所有人可看到彼此的音视频画面
2. 切换摄像头/屏幕共享时，其他端画面正常切换，音频无中断
3. 内网NAT环境下连接成功率≥99%，无首次连接黑屏问题
4. 单服务器CPU占用相比MCU模式降低≥30%

---

### 📌 QM-003 子Issue

**标题**：perf: 完成传输层性能优化与硬件加速适配
**指派对象**：后端开发智能体
**标签**：`performance` `optimization`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-002

#### 任务背景
优化SFU媒体传输性能，提升弱网环境下的会议体验，适配硬件编码降低服务器CPU占用。

#### 交付内容
1. 开启webrtc-rs原生NACK丢包重传、FEC前向纠错机制
2. 适配NVIDIA NVENC / Intel QSV硬件编码加速，支持1080p@30fps视频编码
3. 提供弱网模拟测试脚本，可模拟不同丢包率/延迟场景
4. 输出性能压测报告，包含单服务器承载上限、延迟数据

#### 强制约束
- 弱网优化不牺牲音视频同步性
- 硬件加速为可选配置，无GPU环境自动降级为软编码
- 所有性能指标可复现，压测脚本可直接运行

#### 验收标准
1. 30%丢包率下视频无明显花屏、音频无中断
2. 开启硬件加速后，1080p@30fps编码CPU占用降低≥60%
3. 单服务器可承载≥200路720p@30fps媒体流
4. 端到端音视频延迟≤200ms

---

### 📌 QM-004 子Issue

**标题**：feat: 实现WebSocket轻量信令服务与JWT鉴权
**指派对象**：后端开发智能体
**标签**：`feature` `signaling` `auth`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-002

#### 任务背景
实现标准化WebSocket信令服务，完成SDP协商、ICE候选交换，为多端接入提供统一信令入口。

#### 交付内容
1. 自定义JSON格式轻量信令协议，覆盖SDP offer/answer交换、ICE candidate推送
2. 适配webrtc-rs异步连接时序，解决ICE候选乱序导致的首次连接黑屏问题
3. 基于JWT实现参会者身份鉴权，预留SSO/OAuth对接入口
4. 信令通道强制WSS加密，禁止明文传输
5. 输出信令协议API文档，包含所有消息格式与示例

#### 强制约束
- 信令服务独立部署，与SFU服务解耦
- 未携带有效JWT的连接直接拒绝，无法进入任何会议
- 信令消息延迟≤50ms

#### 验收标准
1. 浏览器端通过WebSocket信令可正常完成SDP协商、ICE连接
2. 首次连接成功率≥99%，无黑屏、连接超时问题
3. 无效JWT请求100%被拦截，无法获取任何会议信息
4. 并发1000个信令连接无服务崩溃、内存泄漏

---

### 📌 QM-005 子Issue

**标题**：feat: 实现房间管理与参会者权限管控
**指派对象**：后端开发智能体
**标签**：`feature` `room` `permission`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-004

#### 任务背景
实现完整的会议房间生命周期管理与参会者权限体系，支撑主持人管控需求。

#### 交付内容
1. 会议房间创建/销毁逻辑，支持设置会议密码、参会人数上限
2. 等候室功能，主持人审核通过后参会者方可进入会议
3. 主持人权限体系：全局静音、移除参会者、设置联席主持人、权限分配
4. 参会者状态实时同步：麦克风开关、摄像头开关、屏幕共享状态
5. 房间资源自动回收：所有人离开后5分钟自动释放房间占用资源

#### 强制约束
- 状态同步延迟≤100ms，所有端状态完全一致
- 非主持人无法执行任何管控操作
- 房间ID全局唯一，无重复冲突

#### 验收标准
1. 可正常创建带密码的会议，输入错误密码无法入会
2. 等候室功能正常，主持人可批准/拒绝参会者进入
3. 主持人操作静音、移除参会者，所有端100ms内同步状态
4. 最后一人离开后5分钟，房间资源被正确释放，无内存泄漏

---

### 📌 QM-006 子Issue

**标题**：feat: 实现分布式集群调度与NATS状态同步
**指派对象**：后端开发智能体
**标签**：`feature` `cluster` `distributed`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-005

#### 任务背景
实现SFU服务集群化部署，支持水平扩容，支撑万人级旁听参会规模。

#### 交付内容
1. 基于NATS实现跨节点房间状态同步，多节点部署房间状态完全一致
2. 房间自动路由调度，新创建会议自动分配到负载最低的媒体节点
3. 节点健康检查，故障节点自动下线，会议自动迁移到健康节点
4. 支持旁听模式，单会议可承载大量只收流不发流的参会者
5. 输出集群部署配置文档与扩容操作说明

#### 强制约束
- 集群节点可动态增减，无需重启整体服务
- 单节点故障不影响其他节点正在进行的会议
- 水平扩容近似线性提升承载能力

#### 验收标准
1. 3节点集群部署，不同会议自动分配到不同节点
2. 单个节点宕机，其上的会议在10秒内自动迁移到其他节点
3. 单会议支持≥10000人旁听，音视频流畅无卡顿
4. 新增节点启动后30秒内接入集群并开始承接新会议

---

### 📌 QM-007 子Issue

**标题**：feat: 对接硅基流动ASR，实现实时语音转写与字幕推送
**指派对象**：后端开发智能体
**标签**：`feature` `ai` `asr`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-005

#### 任务背景
对接本地部署的硅基流动大模型ASR能力，实现会议实时语音转写与字幕推送。

#### 交付内容
1. 从SFU媒体流中提取Opus音频帧，对接硅基流动ASR接口
2. 实现说话人自动区分，不同参会者的发言自动标注身份
3. 通过RTCDataChannel将转写字幕实时推送给所有参会端
4. 支持参会者自主开关字幕显示
5. 全量转写文本本地存储，供后续纪要生成使用

#### 强制约束
- 只调用本地部署的硅基流动接口，禁止调用任何公网第三方ASR服务
- 转写数据完全本地存储，不上传任何外部服务器
- 音频提取不影响原有媒体流传输质量

#### 验收标准
1. 端到端转写延迟≤1秒，音画字幕同步偏差≤300ms
2. 普通话转写准确率≥95%
3. 支持≥4人同时发言，说话人区分准确率≥90%
4. 转写过程中会议音视频无卡顿、延迟升高问题

---

### 📌 QM-008 子Issue

**标题**：feat: 基于大模型实现智能会议纪要自动生成
**指派对象**：后端开发智能体
**标签**：`feature` `ai` `summary`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-007

#### 任务背景
利用本地部署的硅基流动大模型，实现会后自动生成结构化会议纪要。

#### 交付内容
1. 会议结束后自动触发大模型分析全量转写文本
2. 自动提取核心议题、发言要点、会议结论
3. 自动识别待办任务，包含任务内容、负责人、截止时间
4. 支持纪要导出为Markdown、Word两种格式
5. 提供纪要查询接口，支持按会议ID、时间范围检索历史纪要

#### 强制约束
- 所有AI调用仅使用本地部署的硅基流动大模型
- 纪要生成过程不影响正在进行的其他会议
- 纪要数据完全本地存储，满足私有化合规要求

#### 验收标准
1. 1小时会议纪要生成时间≤30秒
2. 核心议题提取准确率≥90%，无明显遗漏
3. 待办任务识别准确，负责人、截止时间提取正确
4. 导出的Markdown/Word文件格式规范、内容完整

---

### 📌 QM-009 子Issue

**标题**：feat: 集成AI音视频增强能力
**指派对象**：前端开发智能体 + 后端开发智能体
**标签**：`feature` `ai` `enhancement`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-005

#### 任务背景
实现AI驱动的音视频增强能力，提升会议音视频质量与体验。

#### 交付内容
1. 端侧轻量人像分割模型，支持虚拟背景、背景模糊功能，本地推理不占用服务器算力
2. 服务端AI智能降噪，过滤环境杂音、键盘敲击声、空调声
3. 可调强度的美颜效果，支持美白、磨皮等基础美颜
4. 所有增强功能可独立开关，用户可自主选择开启/关闭
5. 输出性能影响报告，明确开启增强后的CPU/内存占用变化

#### 强制约束
- 端侧模型体积≤20MB，不显著增加客户端安装包大小
- 开启降噪后音频延迟增加≤50ms
- 虚拟背景抠图边缘自然，无明显锯齿、闪烁

#### 验收标准
1. 背景模糊/虚拟背景效果自然，人物边缘无明显割裂感
2. AI降噪开启后，环境杂音降低≥80%，人声清晰无失真
3. 美颜效果自然，无过度磨皮、脸变形问题
4. 开启所有增强功能后，会议仍保持流畅无卡顿

---

### 📌 QM-010 子Issue

**标题**：feat: 实现AI驱动的弱网自适应码率调整
**指派对象**：后端开发智能体
**标签**：`feature` `ai` `network`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-007

#### 任务背景
通过AI模型预测网络质量，动态调整视频码率，提升弱网环境下的会议体验。

#### 交付内容
1. 每500ms采集一次网络指标：RTT延迟、丢包率、抖动
2. 轻量预测模型根据历史与实时指标预测未来网络变化
3. 动态调整视频码率、分辨率、帧率，弱网环境下优先保障音频流畅
4. 网络恢复后自动逐步提升视频质量，无跳变、闪烁
5. 输出弱网测试报告，对比普通码率控制与AI自适应的体验差异

#### 强制约束
- 模型推理耗时≤10ms，不影响媒体传输实时性
- 码率调整平滑，无忽高忽低的频繁抖动
- 极端弱网下优先保音频，视频可降级为关摄像头

#### 验收标准
1. 10%丢包率下音频无中断，视频流畅无长时间卡顿
2. 网络突然变差时，1秒内完成码率下调，无音视频中断
3. 网络恢复后，5秒内逐步恢复到原有画质
4. 相比传统码率控制，弱网下用户主观体验评分提升≥30%

---

### 📌 QM-011 子Issue

**标题**：docs: 定义客户端-服务端统一API接口文档
**指派对象**：架构师智能体
**标签**：`docs` `api`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-005

#### 任务背景
输出全端统一的API接口文档，为网页、桌面、移动端开发提供标准化接入规范。

#### 交付内容
1. OpenAPI 3.0格式完整REST API文档，覆盖房间、用户、纪要等接口
2. WebSocket信令协议完整定义，所有消息格式、字段说明、示例
3. RTCDataChannel消息格式规范，包含字幕、聊天、控制消息
4. 所有接口的鉴权方式、错误码、限流规则说明
5. curl/Postman调试示例，可直接用于接口测试

#### 强制约束
- 所有接口字段命名统一、语义清晰，无歧义
- 错误码全局唯一，每个错误码有明确的解决方案说明
- 文档可直接导入Postman/ApiFox使用

#### 验收标准
1. 文档覆盖所有已开发的后端接口，无遗漏
2. 每个接口有完整的请求参数、响应字段、示例
3. 前端开发者仅凭文档即可完成对接，无需额外询问后端
4. 导入ApiFox后可直接运行接口调试，全部示例可正常返回

---

### 📌 QM-012 子Issue

**标题**：feat: 实现网页端会议核心功能
**指派对象**：前端开发智能体
**标签**：`feature` `client` `web`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-011

#### 任务背景
开发QuickMeet网页端，支持主流浏览器一键入会，实现完整的会议交互体验。

#### 交付内容
1. 基于React搭建网页前端，支持Chrome、Edge、Safari全主流浏览器
2. 入会前页面：设备自检（摄像头预览、麦克风测试、设备切换）、会议号入会、创建会议
3. 会议中页面：多画面布局切换（画廊模式/演讲者模式/全屏）、音视频开关、屏幕共享、参会者列表
4. 实时字幕展示、文字聊天、会议基础控制功能
5. 响应式适配，支持PC、平板浏览器正常使用
6. 会议结束页面：显示会议时长、纪要生成入口

#### 强制约束
- 无需安装任何插件，打开浏览器即可使用
- 界面风格简洁现代，操作逻辑对齐主流会议软件
- 适配系统暗黑模式

#### 验收标准
1. Chrome、Edge、Safari最新版均可正常入会、音视频互通
2. 所有功能按钮响应正常，无点击无反应、报错问题
3. 4人会议画面切换流畅，无卡顿、布局错乱
4. 屏幕共享功能正常，支持共享桌面、指定窗口、浏览器标签页

---

### 📌 QM-013 子Issue

**标题**：feat: 基于Flutter+Rust FFI实现Android/iOS移动端
**指派对象**：前端开发智能体
**标签**：`feature` `client` `mobile`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-012

#### 任务背景
开发QuickMeet移动端App，基于Flutter+Rust FFI复用核心逻辑，支持Android、iOS双平台。

#### 交付内容
1. Flutter框架搭建，通过Rust FFI封装webrtc-rs核心逻辑
2. 支持Android 8.0+、iOS 13.0+系统
3. 核心功能：入会、音视频开关、前后摄像头切换、屏幕共享、实时字幕、聊天
4. 移动网络优化：码率自适应、弱网提示、后台运行自动关摄像头保音频
5. 功耗优化：1小时会议手机耗电≤15%
6. 移动端适配：竖屏/横屏自动切换、手势操作、通知推送

#### 强制约束
- 核心逻辑复用Rust层代码，不重复造轮子
- 安装包体积：Android≤100MB，iOS≤150MB
- 后台运行不偷跑流量、不异常耗电

#### 验收标准
1. Android、iOS真机可正常安装、入会、音视频互通
2. 移动网络4G环境下会议流畅，1小时耗电≤15%
3. 切换前后摄像头、屏幕共享功能正常
4. 后台切回前台后，音视频自动恢复，无需重新入会

---

### 📌 QM-014 子Issue

**标题**：feat: 基于Tauri实现Windows/macOS桌面客户端
**指派对象**：前端开发智能体
**标签**：`feature` `client` `desktop`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-012

#### 任务背景
开发QuickMeet桌面客户端，基于Tauri封装，底层复用webrtc-rs核心能力，支持Windows、macOS。

#### 交付内容
1. Tauri框架搭建，复用网页端前端代码，底层对接webrtc-rs原生能力
2. 支持Windows 10+、macOS 11+系统
3. 核心功能：系统级屏幕共享（选窗口/全屏/区域）、全局快捷键、系统托盘
4. 本地会议录制功能，录制文件保存到本地磁盘
5. 全局会议提醒、会议日程管理
6. 安装包体积≤100MB

#### 强制约束
- 界面与网页端保持一致，用户操作体验统一
- 启动速度≤2秒，内存占用≤200MB
- 支持自动更新功能

#### 验收标准
1. Windows、macOS可正常安装、启动、入会
2. 系统级屏幕共享正常，可选择指定应用窗口共享
3. 本地录制功能正常，录制文件音画同步、清晰
4. 全局快捷键、系统托盘功能正常

---

### 📌 QM-015 子Issue

**标题**：chore: 统一所有服务Docker镜像编排，适配docker-compose 1.29.2
**指派对象**：运维智能体
**标签**：`chore` `deployment` `docker`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-003（后续随各服务开发持续更新）

#### 任务背景
统一所有服务的Docker镜像构建与编排，实现单命令一键部署整套QuickMeet服务。

#### 交付内容
1. 所有核心服务（SFU、信令、NATS、AI服务、前端）独立Dockerfile，多阶段构建精简镜像体积
2. 统一docker-compose.yml配置，完全兼容docker-compose 1.29.2版本，无需docker-compose-plugin
3. 环境变量配置模板，可自定义端口、密钥、TURN地址、大模型地址等参数
4. 默认端口映射适配内网192.168.0.0/24网段
5. 一键启动、停止、升级脚本，适配MobaXterm SSH操作

#### 强制约束
- 必须兼容docker-compose 1.29.2，禁止使用仅compose v2支持的语法
- 所有镜像基于官方基础镜像构建，无不明来源镜像
- docker-compose up -d一条命令即可拉起全套服务，无需额外手动配置

#### 验收标准
1. 全新服务器环境下，执行docker-compose up -d后5分钟内所有服务启动成功
2. 所有服务健康检查正常，无启动失败、循环重启问题
3. 网页端可正常访问、创建会议、入会互通
4. 修改配置文件重启服务即可生效，无需改代码

---

### 📌 QM-016 子Issue

**标题**：feat: 补全安全合规能力，满足私有化部署要求
**指派对象**：后端开发智能体 + 运维智能体
**标签**：`feature` `security` `compliance`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：所有核心功能开发完成

#### 任务背景
补全全链路安全与合规能力，满足政企私有化部署的数据安全要求。

#### 交付内容
1. 强制全链路加密：媒体流DTLS-SRTP加密、信令WSS加密、管理接口HTTPS加密，关闭所有明文端口
2. 参会者水印功能：昵称水印跑马灯，防止会议内容录屏泄露
3. 完整操作审计日志：所有会议操作（入会、离场、管控、录制）留存日志，保存≥180天
4. 会议数据全本地存储：音视频、转写、纪要全部存在内网服务器，无任何公网上传
5. 密码复杂度校验、登录失败锁定、操作权限最小化配置

#### 强制约束
- 所有加密使用行业标准算法，不使用自研加密方案
- 审计日志不可篡改、不可删除
- 满足等保2.0三级基础安全要求

#### 验收标准
1. 端口扫描无任何明文开放端口，所有传输均加密
2. 水印功能正常，录屏可清晰识别参会者身份
3. 审计日志完整，所有操作可追溯
4. 抓包验证无任何会议数据流出内网

---

### 📌 QM-017 子Issue

**标题**：feat: 搭建可视化监控运维面板
**指派对象**：运维智能体
**标签**：`feature` `ops` `monitoring`
**关联Epic**：QuickMeet 总控Epic
**前置依赖**：QM-016

#### 任务背景
搭建完整的监控运维体系，实现服务状态可视化、异常自动告警，降低运维成本。

#### 交付内容
1. Grafana可视化监控面板，包含：
   - 服务器指标：CPU、内存、带宽、磁盘使用率
   - 会议质量指标：在线会议数、参会人数、平均延迟、丢包率、码率
   - 服务健康状态：各服务存活状态、请求量、错误率
2. 异常告警规则：服务宕机、高丢包率、高CPU占用、磁盘满等场景自动告警
3. 单命令运维脚本：服务启停、日志查看、备份恢复、版本升级
4. 完整部署运维文档：环境要求、部署步骤、常见问题排查、故障处理手册

#### 强制约束
- 监控面板可通过浏览器直接访问，支持权限管控
- 告警支持邮件、企业微信/钉钉等多种通知方式
- 运维命令简单易记，降低运维门槛

#### 验收标准
1. 监控面板可实时展示所有核心指标，数据延迟≤30秒
2. 模拟服务宕机、高CPU等场景，5分钟内收到告警通知
3. 运维脚本可正常执行，单命令完成服务启停、升级
4. 运维文档覆盖90%以上常见问题，可直接按文档排查解决
